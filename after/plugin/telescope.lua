require('telescope').setup({
    defaults = {
        layout_strategy = 'vertical',
        sorting_strategy = "ascending",
        layout_config = {
            prompt_position = "top",
            preview_cutoff = 0,
            mirror = true,
        },
    }
})


