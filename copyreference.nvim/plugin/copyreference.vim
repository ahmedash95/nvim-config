" copyreference.nvim - Vim script entry point
" This file ensures the plugin is loaded in Vim compatibility mode

if exists('g:loaded_copyreference')
    finish
endif
let g:loaded_copyreference = 1

" Plugin metadata
let g:copyreference_version = '1.0.0'
